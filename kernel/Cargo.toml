[package]
name = "ts-os-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
limine = "0.5"
x86_64 = { version = "0.14", features = ["instructions", "abi_x86_interrupt"] }
linked_list_allocator = "0.10"
