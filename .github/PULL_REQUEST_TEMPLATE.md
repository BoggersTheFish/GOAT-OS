## What does this PR do?
Brief description (e.g. fix ISO build, add open/close syscalls, extend docs).

## Verification
- [ ] `make` succeeds
- [ ] `make run` shows "BTFOS Ready" (or `./scripts/verify_boot.sh` passes)
- [ ] No new linter/compile warnings (if applicable)

## Related
Issue # (if any).
